这个算是  📱 Android 完整配置指南

  第一步：安装必要软件

  1. 安装 Termux（提供 Git 环境）
    - 浏览器访问：https://f-droid.org/packages/com.termux/
    - 下载 APK 安装，或在 F-Droid 应用里安装
  2. 安装 Obsidian
    - Google Play 或应用商店搜索安装

  ---
  第二步：Termux 配置 Git 环境

  打开 Termux，依次运行以下命令：

  # 1. 更新软件包
  pkg update && pkg upgrade

  # 2. 安装 Git
  pkg install git

  # 3. 配置 Git 用户信息（和电脑一样）
  git config --global user.name "你的用户名"
  git config --global user.email "你的邮箱"

  # 4. 给 Termux 存储权限（允许访问手机文件）
  termux-setup-storage
  运行后会弹出权限请求，点击允许

  ---
  第三步：克隆仓库到手机

  继续在上面的 Termux 中运行：

  # 进入手机 Documents 目录
  cd /storage/emulated/0/Documents

  # 克隆仓库（注意：仓库名是 "-" 比较特殊，建议重命名）
  git clone https://github.com/guofm-code/-.git "Obsidian Vault"

  看到类似 Receiving objects: 100% 就表示成功了。

  ---
  第四步：在 Obsidian 中打开 Vault

  1. 打开 Obsidian App
  2. 点击左下角的 Manage vaults（管理仓库）
  3. 点击 Open folder as vault（打开本地文件夹）
  4. 导航到：Documents → Obsidian Vault
  5. 点击 Use this folder（使用此文件夹）

  现在能看到你的笔记了！

  ---
  第五步：安装 Obsidian Git 插件

  6. 在 Obsidian 中点击左下角 Settings（设置）
  7. 选择 Third-party plugins（第三方插件）
  8. 关闭 Safe mode（安全模式），确认
  9. 点击 Browse（浏览）
  10. 搜索 Git，找到 Obsidian Git
  11. 点击 Install（安装），然后 Enable（启用）

  ---
  第六步：配置 Obsidian Git 自动同步

  设置 → 社区插件 → Obsidian Git → Options

  基础设置：
  ┌───────────────────────┬──────────────────┬──────────────────┐
  │        设置项         │      填写值      │       说明       │
  ├───────────────────────┼──────────────────┼──────────────────┤
  │ Vault backup interval │ 30               │ 每30分钟自动备份 │
  ├───────────────────────┼──────────────────┼──────────────────┤
  │ Auto pull interval    │ 10               │ 每10分钟拉取更新 │
  ├───────────────────────┼──────────────────┼──────────────────┤
  │ Commit message        │ backup: {{date}} │ 提交信息         │
  └───────────────────────┴──────────────────┴──────────────────┘
  开启自动选项（往下滚动）：

  - Pull updates on startup（启动时拉取）
  - Push on backup（备份时推送）
  - Show status bar（显示状态栏）

  ---
  第七步：关键 - 让 Obsidian Git 调用 Termux 的 Git

  由于安卓限制，Obsidian Git 需要配置 Git 路径：

  1. 在 Termux 中运行：
  which git
  会输出类似：/data/data/com.termux/files/usr/bin/git

  2. 复制这个路径
  3. 回到 Obsidian 设置 → Obsidian Git → 找到 Path to git executable
    - 填入上面复制的路径

  ---
  第八步：测试同步

  4. 在手机上：新建一个测试笔记，写点内容
  5. 等待 30 分钟（或点击左侧栏 Git 图标手动备份）
  6. 看状态栏是否显示 Backup: Done
  7. 在电脑上：运行 git pull，看能否收到手机的更新

  ---
  🔧 日常使用
  ┌────────────────────┬───────────────────────────────────────┐
  │        场景        │                 操作                  │
  ├────────────────────┼───────────────────────────────────────┤
  │ 手动立即同步       │ 左侧栏点击 Git 图标 → Commit and Push │
  ├────────────────────┼───────────────────────────────────────┤
  │ 查看同步状态       │ 看底部状态栏文字                      │
  ├────────────────────┼───────────────────────────────────────┤
  │ 电脑更新后手机获取 │ 重启 Obsidian 自动 pull，或手动 Pull  │
  └────────────────────┴───────────────────────────────────────┘
  ---
  ⚠️ 常见问题

  Q: Obsidian Git 提示 "Git not found"
  - 确保 Termux 已安装 git
  - 确保 "Path to git executable" 填了正确路径

  Q: 同步失败/冲突
  - 先在 Termux 运行：cd /storage/emulated/0/Documents/Obsidian Vault && git status
  - 如果有冲突，先把冲突文件备份，然后 git reset --hard origin/main